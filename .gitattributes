# Normaliza quebras de linha para LF no repositório (evita ruído CRLF em diffs/merges).
* text=auto eol=lf

# Binários — nunca normalizar.
*.wasm binary
*.onnx binary
*.png binary
*.jpg binary
*.ico binary
