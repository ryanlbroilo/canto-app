node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Rust / WASM build (intermediários — o artefato final vai em src/wasm/)
dsp/target
dsp/pkg-node
dsp/Cargo.lock

# binários do ORT (regeneráveis: npm run sync:ort)
public/ort/

# plataformas nativas do Capacitor (geradas por `npx cap add` — regeneráveis)
/android
/ios

# karaokê: separação de voz (regenerável) e faixas importadas.
# As faixas ficam de fora de propósito — um .canto.json é a transcrição da melodia de
# uma gravação comercial. Importar para uso próprio é uma coisa; publicar num repo
# no GitHub é outra. O áudio também nunca entra aqui.
.cache/
public/karaoke/
*.canto.json

# segredos locais (EVA Hub) — NUNCA commitar chaves vivas
.env
.env.local
.env.*.local
!.env.example
